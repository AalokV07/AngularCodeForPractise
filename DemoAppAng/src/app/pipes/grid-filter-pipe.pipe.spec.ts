import { GridFilterPipePipe } from './grid-filter-pipe.pipe';

describe('GridFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GridFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
